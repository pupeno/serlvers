- Add signatures to all the return values of the parsing functions of messages.
- Document the signature of all functions.
- Make resource reccords and message test cases as dynamic as domains and queries test cases.
- Move dns_msg.erl to dns.erl if it doesn't conflict with anything else.